# @matheusbbarni/atelier-linux-arm64

Implementation package for `@matheusbbarni/atelier`.

Do not install this package directly. Install `@matheusbbarni/atelier` globally
and npm will select this package on glibc Linux arm64 when appropriate.
