# Authoring directory for the `docs` content collection (see ../../content.config.ts).
# Committed prose (Quickstart/Concepts/Governance, task_04–06) lands here;
# generated reference (task_11) lands under `_generated/`.
