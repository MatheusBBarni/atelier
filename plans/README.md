# Implementation Plans

Generated by the improve skill on 2026-06-14 at commit `cf40d98`. Execute in the order below unless dependencies say otherwise. Each executor must read the assigned plan fully before starting, honor its STOP conditions, and update the status row when done.

## Execution Order And Status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Stop tracking runtime history | P1 | S | - | TODO |
| 002 | Close relative symlink escapes in harness actions | P1 | M | - | TODO |
| 003 | Apply secret and private path policy to model file actions | P1 | M | 002 | TODO |
| 004 | Require approval for mutating command forms | P1 | M | - | TODO |
| 005 | Harden VCS intent detection against negation | P1 | M | - | TODO |
| 006 | Validate Codex runtime args against policy bypass flags | P2 | M | - | TODO |
| 007 | Clear high-severity web dependency advisories | P2 | M | - | TODO |
| 008 | Align release publishing with the npm distribution contract | P2 | M | - | TODO |
| 009 | Reconcile verification docs and pull request CI | P2 | S-M | 008 | TODO |
| 010 | Retire legacy chat event rendering | P3 | M | - | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency Notes

- 003 depends on 002 because private/secret action policy should run after path resolution cannot escape through relative symlinks.
- 009 depends on 008 because both touch workflow semantics; align release publishing before documenting/enforcing the final CI contract.

## Verification Baseline

Known passing during audit:

```bash
rtk cargo fmt --check
rtk cargo clippy --all-targets
rtk cargo test --locked
rtk npm --prefix npm run check:versions
rtk npm --prefix npm audit --audit-level=high
```

Known audit gap:

```bash
rtk cargo audit
```

This failed because `cargo-audit` is not installed in the local environment.

Known failing during audit:

```bash
rtk npm --prefix web audit --audit-level=high
```

This reported high-severity advisories in the Astro/Vite/esbuild web dependency chain; Plan 007 addresses it.

## Findings Considered And Rejected Or Deferred

- npm package lockfile workspace versions: observed stale-looking `npm/package-lock.json` metadata, but `npm --prefix npm run check:versions` passed and current release scripts read manifests, not those lockfile package entries. Defer unless npm install/release validation starts failing.
- registry integrity comparison on skipped npm publishes: useful hardening, but lower leverage than aligning the release workflow with the tag-only trusted-publishing contract in Plan 008.
- background file-index polling performance: real potential cost in large workspaces, but lower priority than security and release-contract work.
- giant `src/app/mod.rs` / `src/tui/mod.rs` module split: defer broad extraction until Plan 010 removes the legacy chat-rendering coupling and adds projection-first characterization tests.
- direction options from the audit (`ast_search`/docs fetch policy, Codex JSONL events, recent-session navigation): not planned here because the user asked for plans for findings, not roadmap options.

